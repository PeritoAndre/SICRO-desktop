//! Hashing primitives. Placeholder in Spike A — only the module exists so the
//! arrangement matches the architecture doc. SHA256 of media will be wired
//! in Spike D (importador) and Spike F (vídeo).

pub mod sha256;
