//! Intentionally empty in Spike A.
//!
//! When the importer/video spikes need it, add a streaming implementation
//! here (probably wrapping `sha2::Sha256` with a buffered reader). Keeping
//! the module present lets us avoid a future restructure when the dependency
//! is finally pulled in.
